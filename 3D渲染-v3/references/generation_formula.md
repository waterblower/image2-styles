[BASE_STYLE]
Combine `shared_style_invariants.md` with exactly one relevant route base style, or only the actually needed route styles for mixed subjects.

[CONTENT]
Write the user's requested subject, action, setting, and constraints. Keep this layer original; do not copy source reference content unless the user explicitly asks for it.

[LIGHT_COLOR]
Default: soft bright diffused daylight, cool aqua/cyan ambient fill, pale warm neutrals, restrained rose accents, high-key clean contrast, low black point.

[COMPOSITION]
Write only the framing required by the user or the target asset. Do not use composition to imitate the source reference.

[NEGATIVE]
Use `negative_prompt.md`, then remove any negatives irrelevant to the route if they could over-constrain the image.
